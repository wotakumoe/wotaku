/**
 * Tooltip labels for icons without shortcode aliases.
 * Format: 'iconpack-id': 'Description'
 *
 * These are merged with alias labels from emoji.ts
 * to provide hover tooltips on all icons.
 */
export const iconTooltips: Record<string, string> = {
  'mingcute-server-fill': 'Self-uploaded (Own storage or video host)'
}
