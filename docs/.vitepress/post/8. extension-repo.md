---
type: component
title: Extension Repos
---

We now have a dedicated extension repo component instead of the default markdown table, which should give you a much cleaner expereince. You can look through every repo and filter them based on language, types and other tags (based on source repo maintainer).

![](/post/extrepo/img1.png)